cron:
- description: "Cron job to send pub sub message to subzero which runs cron jobs on gcp in response"
  url: /cron
  target: beta
  schedule: every 1 minutes
env_variables:
  PUBSUB_VERIFICATION_TOKEN: sdfgsdlfgAsssfsfs  